# pili++ 项目协作约定

## 1. 项目定位

本仓库基于开源项目
[`bggRGjQaUbCoE/PiliPlus`](https://github.com/bggRGjQaUbCoE/PiliPlus)，
定位是“上游 PiliPlus + 少量长期维护的 Android 差异”，不是独立发展全部产品功能的 fork。

本项目只长期维护三类用户功能差异：

1. 在 Android 上用 AndroidX Media3 ExoPlayer 完整替代 mpv；
2. 视频详情页下拉进入竖屏全屏、竖屏全屏上滑退出及其连续动画交互；
3. 复用同一播放器会话和 Flutter `Texture` 的应用内小窗。

搜索、推荐、动态、评论、账号、收藏、下载、普通 UI、业务接口及其他通用功能均以上游实现为准。
除非用户明确扩大范围，不在本仓库平行重写、长期分叉或自建替代实现；发现通用问题时，优先接受、
等待或向上游贡献修复。

产品名称、Android applicationId、签名、Android-only CI、发布校验和项目文档是维持上述三类差异
可构建、可安装、可验证所需的支撑性改造，不构成第四类产品功能。

## 2. 文档职责与事实优先级

- 本文件只记录长期有效的范围、架构、同步、验收和发布规则。
- `docs/current_state.md` 记录当前 Git/上游基线、未提交修改、最近交付、验证结果、用户决定、
  已知缺口和下一步；每次开始任务前必须读取，结束前必须更新。
- `docs/android_exoplayer.md` 记录 Media3 兼容清单、实现历史和真机验收事实。
- `docs/android_mpv_removal.md` 记录 Android 完整移除 mpv 的阶段计划和最终门槛。
- 文档与实际 Git、构建产物或用户最新指令不一致时，以可验证事实和用户最新指令为准，并在本批
  结束前修正文档。
- 不把临时故障、当前 commit、一次性 APK 路径或短期计划固化在本文件中。

## 3. 产品身份与平台范围

- 用户可见软件名固定为 `pili++`，Debug 构建可显示为 `pili++ debug`。
- Android applicationId 和 namespace 固定为 `com.shudo.plusplus`；现有 Java/Kotlin package
  保持为 `com.example.piliplus`，不得混淆。
- 除非用户明确授权并提供迁移方案，不修改 applicationId、package、数据目录或签名证书。
- 官方 PiliPlus 与 `pili++` 的 applicationId 不同，二者不能互相覆盖安装；`pili++` 后续交付
  必须保持 applicationId 和签名一致，保证已有安装可以升级。
- 当前只构建、测试和交付 Android。iOS、macOS、Windows、Linux 目录和条件代码为同步上游而保留，
  不作为当前支持平台，也不应在普通清理中删除。

## 4. 三类本地差异的边界

### 4.1 Android Media3 完整替代 mpv

“替代”不是只让视频播放，而是 mpv 原有用户能力在 Media3 路径上具有等价结果：

- 原入口、控件、手势、弹幕、字幕、章节、轨道、截图和业务副作用必须保留；
- 播放、暂停、seek、完成、缓冲、倍速、音量、错误和换源状态必须正确回传 Flutter；
- 点播、DASH、直播、独立音频、本地文件、UGC、PGC、课程、合集和互动视频均需闭环；
- 音频焦点、通知、媒体键、耳机/蓝牙、后台、系统 PiP 和生命周期行为需等价；
- Media3 没有直接对应能力时，在 Kotlin 桥接层或 Flutter 公共层实现等价结果。

以下做法不算完成：

- 通过 `if (!useExoPlayer)` 隐藏入口或控件；
- 留空、只记录日志、静默返回或以“暂不支持”作为最终方案；
- 出错后静默回退 mpv，并把回退成功当成 Media3 已适配；
- 只实现原生播放，未接通 Flutter 控件、状态和业务回调；
- 只通过编译、模拟器或自动化就宣称兼容完成。

在 APK 不再包含 mpv 原生库、Android 不再创建或调用 mpv、兼容矩阵完成真机验证之前，不得宣称
“mpv 已被完整替代”。无法当前完成的项目必须明确记录原因、影响范围和下一步。

### 4.2 视频页下拉竖屏全屏交互

- 窗口态详情页只有在真实滚动顶部的有效下拉才可进入竖屏全屏；评论区和非顶部滚动不得误触。
- 竖屏全屏视频区域上滑可退出，短滑回弹；锁定、多指、反方向和以横向为主的手势不得触发。
- 播放器尺寸、位置、详情面板和必要的圆角变化应由同一动画进度驱动，避免端点二次跳变。
- 不破坏播放器既有横向 seek、纵向亮度/音量、双击、长按、缩放、锁定和旋转手势。
- 交互状态机和动画尽量位于独立组件；视频页只保留薄的挂接点，避免复制或长期冻结上游页面结构。

### 4.3 应用内小窗

- 开关默认关闭；关闭时立即释放已有应用内小窗会话。
- 从视频页返回时复用同一播放器实例和 Flutter `Texture`，不得重建会话、重载媒体、跳转进度或
  产生可见黑帧。
- 小窗按当前视频真实宽高比适配，支持拖动、播放/暂停、VOD 前后十秒、关闭和恢复；不显示
  “系统画中画”按钮。
- 缩小和恢复时，位置、尺寸、圆角与页面接管使用一致的动画进度；恢复页面与小窗归位并行，
  页面接管播放器后再移除 Overlay。
- 恢复参数来自播放器和视频控制器当前状态，覆盖 UGC 分P、PGC 剧集、互动视频和本地文件。
- 打开下一个视频或直播前释放旧小窗；应用内小窗进入系统 PiP 后，PiP 全屏恢复当前视频页，
  关闭 PiP 不得主动打开视频页。
- 页面只负责“交出播放器”和“接回播放器”；会话所有权、Overlay、路由恢复和 PiP 协同集中在
  小窗服务和后端中立接口中。

## 5. 架构边界与冲突面控制

- 页面、业务、控件、手势、弹幕、字幕和覆盖层保持 backend-neutral 的 Flutter 实现。
- mpv 与 Media3 通过统一播放器接口/adapter 提供状态和操作，页面不得直接读取后端对象。
- Android Media3 使用 Flutter `Texture`；不使用遮挡 Flutter 控件的 `AndroidView`、原生
  `PlayerView` 或独立原生控制层。
- Kotlin 桥接负责 Media3 会话、媒体源、轨道、Surface、音频处理、生命周期和事件；Flutter
  adapter 将其映射到公共播放器语义。
- 新代码不得扩大对 `Player`、`NativePlayer`、`VideoController` 或 mpv 命令的直接访问。
  发现依赖时迁移到公共接口，或隔离到非 Android mpv adapter 并记录债务。
- Media3、下拉交互和小窗实现应分别拥有清晰模块；上游热点文件只保留最小 hook/wrapper，禁止
  为保留本地功能长期复制整份上游页面或控制器。
- 普通上游功能不得反向依赖三类本地实现；本地 adapter 也不得改变无关页面的业务语义。

## 6. 上游同步策略

### 6.1 基本原则

- 上游远程固定为 `https://github.com/bggRGjQaUbCoE/PiliPlus.git`。
- 除三类本地功能和必要支撑改造外，上游是产品功能与普通 UI 的事实来源。
- 小批、频繁同步；上游触及播放器、视频页、路由、依赖或生命周期时优先评估，避免长期积累。
- 使用临时 `sync/upstream-<date>` 分支执行同步审查，验证通过后再合入主分支。
- 已发布或共享的主分支优先使用普通 merge 保留同步边界，不为整理历史随意 rebase 或强推。
- 可启用 `git rerere` 辅助复用历史解决方案，但必须人工审查；不得给 Dart/Kotlin 源码配置会静默
  选用 `ours` 的 merge driver。

### 6.2 同步前

1. 读取 `docs/current_state.md`，确认工作区、当前任务和上次同步事实。
2. 工作区有未提交修改时不得直接 merge/rebase；先提交到明确分支，或停止并与用户确认。
3. `git fetch upstream` 后记录本地 HEAD、`upstream/main`、merge-base、双方新增提交和文件范围。
4. 分别列出上游改动、本地三类差异改动及其重叠文件；先审查重叠语义，再开始合并。
5. 不在本文件硬编码“最新上游提交”；具体基线写入 `docs/current_state.md`。

### 6.3 冲突处理优先级

- 搜索、推荐、动态、评论、账号、收藏、下载、普通 UI 和业务接口：默认接受上游实现。
- 上游重写视频页或播放器 UI：接受上游新结构，再把 Media3、下拉交互和小窗的最小 hook 重新
  挂接；不得用旧本地整文件覆盖上游。
- Media3 原生桥接、公共后端接口、下拉状态机、小窗会话所有权：保留本地已验证语义，同时适配
  上游调用约定。
- applicationId、namespace、应用名、签名和发布身份：保留 `pili++` 约定。
- `pubspec.yaml`、`pubspec.lock`、Gradle、Manifest、plugin registrant 和生成文件：逐项核对最终
  语义并重新生成必要产物，禁止整文件简单选择一侧。
- 不得把上游已有功能记为 `pili++` 自研，也不得在同步时以接受上游为由覆盖三类已验证行为。
- 无法判断冲突语义时，停止合并并报告双方实现、影响范围和可选处理方式，不凭猜测完成。

### 6.4 同步后的验证

基础门禁：

- `dart format --output=none --set-exit-if-changed lib test`；
- `dart analyze` 和完整 Flutter 测试；
- Android `:app:testDebugUnitTest`；
- Android Release 构建与 `git diff --check`。

按上游触及范围增加真机回归：

- 播放器/控制器：mpv 与 Media3 的点播、直播、独立音频、切源和控制层；
- 视频页/滚动/手势：下拉竖屏全屏、上滑退出和既有播放器手势；
- 路由/Navigator：小窗创建、恢复、A→B→C 返回链和打开新媒体；
- Activity/生命周期：前后台、系统 PiP、小窗进入 PiP、通知和会话释放；
- 音频服务：焦点、后台、媒体键、耳机和蓝牙；
- Flutter/Gradle/依赖：JVM 测试、每个目标 ABI 的 Release 构建及身份/签名检查。

自动化通过不能替代真机验收；未执行的场景必须明确写为“待真机验证”。

## 7. 工作批次与完成定义

- 每批只处理一个可独立验证的闭环；不要把上游同步、三类功能重构、版本发布和无关清理混成
  一个不可回滚的提交。
- 提交应按领域保持清晰，例如 `media3:`、`portrait-pull:`、`mini-player:`、`sync:`、`release:`、
  `docs:`；不要求改写既有历史。
- 开始播放器批次前，先核对 mpv 基线行为、Media3 事件链和所有调用点。
- 排查事件链应覆盖：Flutter 命中测试 → 控制层/交互状态 → 公共控制器 → MethodChannel →
  Media3 → EventChannel → Flutter 状态与业务副作用。
- 异步操作必须防止重复创建、释放后回调、旧 generation/session 污染新媒体和通道竞态；不得
  吞异常，错误需包含会话、媒体源阶段和 Media3 原因，并提供用户可理解提示。
- “画面能播放但控制层无法显示或操作”属于 P0 回归，完成修复和对应真机验证前不得继续宣称
  点播交互完成。

一项功能只有同时满足以下条件才可标记完成：

1. 用户可见行为与基线等价；
2. 控件、手势、状态流和业务副作用闭环；
3. 三类本地功能彼此及上游普通功能没有回归；
4. 覆盖普通窗口、全屏及适用的前后台、小窗和 PiP；
5. 格式化、静态分析、相关测试和 Android JVM 测试通过；
6. Android Release 构建通过；
7. 在真实 Android 设备上完成适用场景验证；
8. 没有新增隐藏入口、空实现、最终态“暂不支持”或静默 mpv 回退。

## 8. 修改与清理约束

- 工作区可能包含用户或前序任务修改；不得重置、覆盖或顺手清理无关内容。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令清理工作区。
- 删除代码前证明其不可达、已被替代或不属于三类本地差异；不能为减少 diff 删除上游用户功能。
- 普通功能的本地魔改、临时补丁和重复模型应优先清理或上游化；三类差异应通过 adapter/hook
  缩小冲突面，而不是堆叠页面级条件分支。
- 不以更换第三方 UI 控件绕过上游功能；优先复用上游页面与现有 Flutter 播放器 UI。
- 新增 Media3 依赖时检查版本兼容、APK 体积、混淆、最低 Android 版本和许可证。
- 除非用户明确要求，不修改上游地址、许可证、应用身份或用户数据格式。

## 9. 打包、签名与交付

- 需要交付时构建 Android Release APK；产物必须来自完成验证的准确源码状态，并记录版本、
  构建时间和 Git commit/diff。
- 每次正式交付的 Android `versionCode` 必须高于全部既有交付包；`versionName`、`versionCode`
  和文件名必须一致，不得无提示覆盖同名 APK。
- Release 必须使用项目约定的同一证书。当前证书 SHA-256 基线为
  `775803BD534E2A0984CF8E7796DCF1D82FD7D436F10A1FEDA77C6981F4C44C5C`；不同则停止交付。
- 构建后核验 applicationId、应用名、versionName、versionCode、ABI、签名和 APK SHA-256。
- 正式交付前必须运行：
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tool/verify_release.ps1 -ApkPath <apk>`。
  `tool/release_baseline.json` 只有在新产物通过校验并实际交付后才能更新。
- APK 文件名应包含 `pili++`、版本、ABI 和构建类型。
- keystore、`key.properties`、store password、key alias 和 key password 属于敏感信息，不得提交、
  写入文档、粘贴到公开日志或放入公开交付说明。
- 覆盖、替换或重新生成证书前必须取得用户确认；用户接管签名时，证书与四项签名信息应通过
  明确的本地未跟踪文件或用户指定安全渠道一并交付。

## 10. 汇报与状态维护

每批结束时用可验证事实说明：

- 本批属于 Media3、下拉竖屏全屏、应用内小窗、上游同步还是支撑性维护；
- 闭环了哪些用户场景，修改了哪些关键文件；
- 执行了哪些自动化、构建和真机验证；
- 若有产物，给出完整路径、版本、ABI、签名状态和 SHA-256；
- 三类本地差异仍有哪些缺口，哪些场景仍待真机验证；
- 本批是否引入了新的上游冲突面，以及下一步如何缩小。

结束前同步更新 `docs/current_state.md`。不要用“基本支持”“应该可以”“理论上没问题”替代测试
结果，也不要把未执行的检查写成通过。
